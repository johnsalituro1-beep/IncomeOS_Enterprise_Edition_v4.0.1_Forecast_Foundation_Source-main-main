import { BriefcaseBusiness, Building2, CalendarClock, CheckCircle2, FileText, ShieldCheck, Users } from 'lucide-react'
import { advisorSummary, advisorWorkspace, householdSummary } from '../domain/advisor/advisorEngine'
export function StageSixAdvisorPage(){const s=advisorSummary(advisorWorkspace); return <section className="stage-shell"><header className="stage-hero"><div><span className="eyebrow">VERSION 12 · STAGE 6</span><h1>Advisor Platform</h1><p>Multi-client, household, CRM, permissions, and professional reporting foundation.</p></div><div className="stage-badge"><ShieldCheck size={18}/> Architecture ready</div></header>
<div className="metric-grid five"><article><span>Total AUM</span><strong>${(s.aum/1e6).toFixed(2)}M</strong></article><article><span>Weekly income</span><strong>${s.weeklyIncome.toLocaleString()}</strong></article><article><span>Clients</span><strong>{s.clients}</strong></article><article><span>Reviews due</span><strong>{s.reviewDue}</strong></article><article><span>Average OS score</span><strong>{s.avgScore}</strong></article></div>
<div className="stage-grid two"><article className="terminal-card"><h2><Users size={18}/> Client command center</h2><div className="data-list">{advisorWorkspace.clients.map(c=><div key={c.id}><div><strong>{c.name}</strong><span>{c.riskProfile} · next review {c.nextReview}</span></div><b className={`pill ${c.status}`}>{c.status}</b></div>)}</div></article>
<article className="terminal-card"><h2><Building2 size={18}/> Household aggregation</h2><div className="data-list">{advisorWorkspace.households.map(h=>{const m=householdSummary(h,advisorWorkspace.portfolios);return <div key={h.id}><div><strong>{h.name}</strong><span>{m.accounts} accounts · OS {m.score}</span></div><b>${m.weeklyIncome.toLocaleString()}/wk</b></div>})}</div></article></div>
<div className="stage-grid three"><article className="terminal-card"><h2><CalendarClock size={18}/> Advisor tasks</h2>{advisorWorkspace.tasks.map(t=><p key={t.id}><b>{t.title}</b><br/><small>{t.priority} priority · due {t.due}</small></p>)}</article><article className="terminal-card"><h2><BriefcaseBusiness size={18}/> CRM layer</h2><ul><li>Client profiles and goals</li><li>Meeting and review history</li><li>Action items and advisor notes</li><li>Household relationship model</li></ul></article><article className="terminal-card"><h2><FileText size={18}/> Professional reporting</h2><ul><li>Portfolio and household summaries</li><li>Income and risk reports</li><li>Digital Twin scenarios</li><li>White-label report contracts</li></ul></article></div>
<article className="terminal-card readiness"><h2><CheckCircle2 size={18}/> Stage 6 delivery</h2><div className="readiness-grid">{['Multi-client domain','Household aggregation','Role model','CRM foundation','Advisor notes','Task center','Client workspaces','White-label contracts','Professional reports'].map(x=><span key={x}>✓ {x}</span>)}</div></article></section>}
