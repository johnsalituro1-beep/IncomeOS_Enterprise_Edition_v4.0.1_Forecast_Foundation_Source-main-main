import type { GeneratedStrategy, StrategyBuilderProfile, StrategyConstraints } from '../../domain/strategy-builder/types'
export type StrategyReport={schemaVersion:'1.0';generatedAt:string;disclaimer:string;profile:StrategyBuilderProfile;constraints:StrategyConstraints;strategies:GeneratedStrategy[]}
export function createStrategyReport(profile:StrategyBuilderProfile,constraints:StrategyConstraints,strategies:GeneratedStrategy[]):StrategyReport{return{schemaVersion:'1.0',generatedAt:new Date().toISOString(),disclaimer:'Educational planning model only. Modeled ETF inputs are placeholders and are not investment advice, forecasts, or verified live market data.',profile,constraints,strategies}}
export function downloadStrategyReport(report:StrategyReport){const blob=new Blob([JSON.stringify(report,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=`income-strategy-report-${new Date().toISOString().slice(0,10)}.json`;a.click();URL.revokeObjectURL(url)}
